# Encoding
set-environment -g LANG en_US.UTF-8
set-environment -g LC_ALL en_US.UTF-8

# Prefix
set -g prefix C-t

# Hides inactive windows
# set -g window-status-format ''
# set -g window-status-current-format ' #W '
set -g window-status-separator ''

# Trying fzf-tmux
bind -n C-w display-popup -E "tmux list-windows -aF '#S:#I #W' | fzf | awk '{print \$1}' | xargs tmux switch-client -t"
# bind w display-popup -E "tmux list-windows -aF '#S:#I #W' | fzf --preview='tmux capture-pane -t {1} -p' | awk '{print \$1}' | xargs tmux switch-client -t"

# Display
set -g set-titles off
set -g pane-border-format '#{pane_index}'
set -g status-left '[#S] '
set -g status-right ''
# set -g status-style 'bg=green,fg=black'

# Tree view
# bind w choose-tree -wF '#{window_name}'

# Reload
bind r source-file ~/.config/tmux/tmux.conf \; display "reloaded"

# Splits (keep current path)
bind b split-window -h -c "#{pane_current_path}"
bind v split-window -v -c "#{pane_current_path}"
bind T split-window -f -l 15 -c "#{pane_current_path}"

# Pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Global last-window: tracks session:window across sessions
set-hook -g after-select-window    "run-shell 'sh ~/.config/tmux/tmux-last-window.sh track'"
set-hook -g client-session-changed "run-shell 'sh ~/.config/tmux/tmux-last-window.sh track'"
bind -n C-s run-shell "sh ~/.config/tmux/tmux-last-window.sh jump"

# Mouse
set -g mouse on

# Copy mode (vi)
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
set -g set-clipboard on
set -g set-clipboard off
if-shell 'test "$(uname -s)" = Darwin' 'bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "pbcopy"' 'bind-key -T copy-mode-vi y send -X copy-pipe-and-cancel "xclip -i -selection clipboard"'

# SSH agent
set -g update-environment "SSH_ASKPASS SSH_AUTH_SOCK SSH_AGENT_PID SSH_C"

# Quick nav
bind f send-keys "gt ~/workspace/" Enter
bind -n M-1 switch-client -t logs:work-log
bind -n C-= switch-client -t sre:shell

# Sessions
run-shell "bash ~/.config/tmux/tmux-sessions.sh"
set -s command-alias[18] session-start='run-shell "bash ~/.config/tmux/tmux-session-manage.sh start"'
set -s command-alias[19] session-stop='run-shell "bash ~/.config/tmux/tmux-session-manage.sh stop"'
