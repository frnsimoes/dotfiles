# Encoding
set-environment -g LANG en_US.UTF-8
set-environment -g LC_ALL en_US.UTF-8

# Prefix
set -g prefix C-t

# Hides inactive windows
# set -g window-status-format ''
# set -g window-status-current-format ' #W '
set -g window-status-format '#I:#W#F '
set -g window-status-current-format '#I:#W#F '
set -g window-status-separator ''


# Display
set -g set-titles off
set -g status-left '[#S] '
set -g status-right ''
setw -g automatic-rename off
# set -g status-style 'bg=green,fg=black'

# Subtle active-pane hint: dim inactive borders, brighter active one
set -g pane-border-style        'fg=colour240'
set -g pane-active-border-style 'fg=colour10'

# Reload
bind r source-file ~/.config/tmux/tmux.conf \; display "reloaded"

if-shell '! tmux has-session -t logs 2>/dev/null' { \
  new-session -d -s logs -n work-log     -c ~/me/notes 'nvim work-log.md' ; \
  new-window  -d -t logs -n personal-log -c ~/me/notes 'nvim personal-log.md' ; \
  new-window  -d -t logs -n papers       -c ~/me/notes 'nvim papers.md' \
}

# 'shell' is the landing session: `tmux` (bare) attaches here via the zsh wrapper.
if-shell '! tmux has-session -t shell 2>/dev/null' { \
  new-session -d -s shell -c ~ \
}

# New windows/splits keep the current pane's path
bind c new-window -c "#{pane_current_path}"
bind b split-window -h -c "#{pane_current_path}"
bind v split-window -v -c "#{pane_current_path}"
bind T split-window -f -l 15 -c "#{pane_current_path}"

# Disposable windows: `o` opens one in the current session, `O` kills them all.
bind o new-window -n disposable -c "#{pane_current_path}"
bind O run-shell "$HOME/workspace/dotfiles/bin/tmux-clean-disposable"

# Pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Window navigation
# Prefix+w is tmux's built-in session/window tree.
unbind-key -n C-w
bind w choose-tree -Zw -F '#{?pane_format,#{pane_index}: #{pane_current_command},#{?window_format,#{window_index}: #{window_name}#{window_flags},#{session_name}: #{session_windows} windows#{?session_attached, (attached),}}}'

# C-s: session chooser (was prefix-s).
bind -n C-s choose-tree -Zs

# Global last-window: tracks session:window across sessions.
# F1 toggles back and forth between the two most recent windows.
# set-hook -g after-select-window    "run-shell 'sh ~/.config/tmux/tmux-last-window.sh track'"
# set-hook -g client-session-changed "run-shell 'sh ~/.config/tmux/tmux-last-window.sh track'"
# bind -n F1 run-shell "sh ~/.config/tmux/tmux-last-window.sh jump"

bind u last-window

# Mouse
set -g mouse on

# Copy mode (vi)
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
set -g set-clipboard on
set -g set-clipboard off
# Use copy-pipe (not -and-cancel) so we stay in copy mode after yanking
if-shell 'test "$(uname -s)" = Darwin' 'bind-key -T copy-mode-vi y send -X copy-pipe "pbcopy"' 'bind-key -T copy-mode-vi y send -X copy-pipe "xclip -i -selection clipboard"'

# SSH agent
set -g update-environment "SSH_ASKPASS SSH_AUTH_SOCK SSH_AGENT_PID SSH_C"

# Quick nav
bind f send-keys "gt ~/workspace/" Enter
bind -n F1 switch-client -t personal:0
bind -n M-1 switch-client -t logs:work-log
bind -n M-2 switch-client -t logs:personal-log
bind -n C-= switch-client -t sre:shell

# Pi
set -g extended-keys on
set -g extended-keys-format csi-u
